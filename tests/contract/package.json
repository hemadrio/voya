{
  "name": "@travel/contract-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-service contract and schema compatibility tests for the travel platform.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@travel/contracts": "workspace:*",
    "@travel/supplier-port": "workspace:*",
    "@travel/test-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
