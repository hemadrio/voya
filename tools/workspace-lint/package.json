{
  "name": "@travel/workspace-lint",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace validation CLI — enforces catalog usage, workspace protocol, and engines consistency.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0 <21.0.0"
  },
  "bin": {
    "workspace-lint": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo 'no lint for workspace-lint'",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
