{
  "name": "@travel/fixture-service",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal fixture service for validating the Forge Shipping pipeline end-to-end (WO-084). Not deployed.",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=20.0.0 <21.0.0"
  }
}
