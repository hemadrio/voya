{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "description": "Next.js 14 App Router web client. Migrated onto @travel/contracts as the single source of truth for request/response validation (WO-004). All platform payload types and schemas are imported from contracts; no local duplicates.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@travel/contracts": "workspace:*",
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.5.3",
    "vitest": "^1.6.0"
  }
}
