# Forge Implementation Log

| Field | Value |
|-------|-------|
| Project | 40ac2648-20da-4cb9-843b-a9ea0b666bfa |
| Branch | forge/voya-9ddc7ab4-run25-106wo |
| Started | 2026-07-31T05:02:15Z |

---

## WO-002: User Story: WO-002 - Standard error envelope and HTTP status mapping contract
- **Status:** completed
- **Commit:** `7d18785`
- **Files:** 24 (+1574/-0)
- **Duration:** 930ss
- **Approach:** Added the platform-wide error contract to @travel/contracts as a self-contained errors/ module. envelope.ts defines the Zod strict schema producing exactly {error:{code,message,field?},reference}. codes.ts declares ErrorCode as a const-union and ERROR_STATUS_MAP as Record<ErrorCode,number> (compile-time exhaustive). domain-errors.ts exports 11 typed factory helpers. serialise.ts implements three dispatch paths (ZodError→VALIDATION_FAILED, DomainError→code lookup, unknown→fixed INTERNAL_ERROR) with a RESTRICTED_FIELDS redaction list mirroring Pino paths, and a sortable fallback correlation ID so the package stays free of OpenTelemetry. shared/middleware/errorHandler.ts is a thin Express adapter calling serialiseError and writing status+JSON. Unit tests cover envelope shape, exhaustive status mapping, serialiser paths, per-field redaction, and leakage prevention. An integration test in booking-service wires CreateBookingRequestSchema failures through serialiseError end-to-end.

## WO-004: User Story: WO-004 - Migrate Next.js frontend onto shared contract types
- **Status:** completed
- **Commit:** `aa67802`
- **Files:** 23 (+1355/-4)
- **Duration:** 811ss
- **Approach:** Built the frontend contracts migration in five layers: (1) configuration — tsconfig.json (strict, react-jsx, bundler resolution) and next.config.js (transpilePackages for workspace ESM); (2) types/index.ts — presentational-only types (BookabilityState, FormErrorState, ApiResult discriminated union) with zero platform payload shapes; (3) pure TypeScript library modules — api-client.ts (typed fetch wrapper returning ApiResult<T>, parses 2xx against contracts schemas and non-2xx against ErrorEnvelopeSchema, synthesises valid envelope for non-JSON gateway responses), error-mapper.ts (mapEnvelopeToFormErrors with top-level segment matching and form-level banner fallback), offer-guard.ts (getBookabilityState / canInitiateCheckout gating ILLUSTRATIVE and expired offers structurally); (4) App Router pages and OfferCard component — all importing types exclusively from @travel/contracts; (5) unit tests for all three library modules and fixture JSON files for search responses, offer lists, and error envelopes.

## WO-005: User Story: WO-005 - Contract compatibility test harness and CI gate
- **Status:** completed
- **Commit:** `bce0c0b`
- **Files:** 70 (+2874/-5)
- **Duration:** 2000ss
- **Approach:** Built the contract compatibility harness in five layers: (1) schema registry — src/registry.ts maps 23 stable dot-notation IDs to every public Zod schema, with a completeness test asserting no gaps or stale baselines; (2) Zod-to-JSON-Schema converter — scripts/zod-to-json-schema.ts implements a recursive Zod _def inspector producing deterministic, sorted JSON Schema fingerprints covering all Zod types used in the codebase (ZodEffects/Pipeline unwrapping, ZodObject strict-mode, ZodEnum sorted values, ZodArray minItems, ZodRecord, ZodUnion oneOf); (3) committed baselines — 23 JSON Schema baseline files in contract-baselines/ generated by tracing each schema through the converter, plus a phase0-metrics.json recording the pre-harness state; (4) test infrastructure — comparator test regenerates every baseline in memory and fails with the schema ID and regeneration instructions on any divergence; classifier.ts categorises diffs as additive/breaking/no-op with a version gate that rejects breaking changes without a major-version bump; test-helpers.ts isolates all vitest assertions so a runner switch only updates one file; (5) consumer fixture tests — contracts.consumer.test.ts added for all 9 services and the frontend, each parsing its specific fixture files against the schemas it depends on, so a narrowing change fails at the consumer rather than only in the contracts package.

## WO-006: User Story: WO-006 - Build @travel/observability Pino logger with PII redaction
- **Status:** completed
- **Commit:** `2376a8a`
- **Files:** 17 (+1200/-5)
- **Duration:** 1119ss
- **Approach:** Created the @travel/observability workspace package with a Pino JSON logger factory and exhaustive PII redaction, then enabled the no-console ESLint rule across all package/service source directories. The package has three source modules: context.ts (typed LogContext interface with no index signature), logger.ts (createLogger + createChildLogger with LOG_LEVEL validation, ISO-8601 timestamps, base bindings, redact config at construction, and a custom error serializer that scrubs email and digit patterns from stack strings), and index.ts (barrel). Tests use Jest 29 + ts-jest in CommonJS mode to avoid ESM/Jest experimental flags while the package itself ships as ESM. An ADR records the two ratified parameters: 30-day CloudWatch application log retention (explicitly distinct from the 1-year immutable audit record store) and Jest 29 as the epic test runner. The root eslint.config.js and per-package .eslintrc.cjs files enforce no-console across all source directories. shared/middleware/errorHandler.ts was updated to accept an optional ErrorLogger injection parameter to enable structured error logging (wired fully in WO-007).

## WO-013: User Story: WO-013 - Secrets Manager Injection, KMS Rotation, and Leak Gating
- **Status:** completed
- **Commit:** `58bcd6e`
- **Files:** 27 (+2113/-6)
- **Duration:** 817ss
- **Approach:** Implemented the full secrets-injection and rotation infrastructure in three layers: (1) Terraform modules — kms (one CMK per store with explicit key policies), secrets (11 logical credentials with lifecycle ignore_changes and 90-day rotation), ecs-service (task definition with secrets block + least-privilege execution role IAM); (2) staging/production environment roots wiring the modules together with S3+DynamoDB Terraform state backends and data-source-driven ECS cluster/subnet lookups; (3) @travel/auth TypeScript package exposing a dependency-injected KeyProvider with two-phase JWT rotation (24-hour overlap window), in-memory cache with configurable TTL, and a SecretsManagerKeyStore production implementation. Added scan:gitleaks as a blocking parallel pipeline step alongside semgrep/snyk/sonarqube/grype, plus Terraform plan stages. Security docs and a .env build-context assertion script round out the deliverables.

## WO-015: User Story: WO-015 - SSRF Egress Allow-List for Supplier and Tool Calls
- **Status:** completed
- **Commit:** `89949e3`
- **Files:** 18 (+1727/-0)
- **Duration:** 947ss
- **Approach:** Created the @travel/suppliers workspace package as the single egress control point. egressPolicy.ts defines a typed AllowedDestination list (9 entries covering Amadeus, RapidAPI wildcard, Stripe, Anthropic, Google OIDC x2, SES, internal gateway) and pure assertAllowedDestination() with no I/O — validates host against allow-list, rejects IP literals and URL userinfo, checks scheme and port. ipRanges.ts implements pure IP-range validators for all blocked ranges. circuitBreaker.ts is a CLOSED/OPEN/HALF_OPEN state machine with injectable clock. hardenedClient.ts wires everything: pre-flight URL validation → DNS pre-resolution + IP validation → agent-level lookup hook (TOCTOU protection at connect time) → manual redirect re-validation with hop cap → 2200 ms timeout → circuit breaker. The internal gateway destination carries allowPrivateAddresses: true because ECS Service Connect resolves to VPC-private addresses by design. Added EGRESS_DENIED (502) to @travel/contracts with egressDenied() factory. Pipeline gains scan:no-direct-fetch Semgrep step blocking direct axios/fetch/http.request calls outside the egress module. Terraform network module adds private-subnet route tables with NAT-only default routes and a Terraform check block asserting no internet-gateway routes exist on private subnets.

## WO-025: User Story: WO-025 - Define SupplierPort abstraction with egress allow-list
- **Status:** completed
- **Commit:** `80628a5`
- **Files:** 16 (+1412/-0)
- **Duration:** 1008ss
- **Approach:** Created the @travel/supplier-port package as the hexagonal-architecture port governing all outbound supplier HTTP calls. The package has four primary modules: (1) errors.ts — typed SupplierError hierarchy mapping to HTTP semantics; (2) EgressAllowList.ts — two-layer config-driven + SSRF pre-flight check with fail-closed construction and security event emission; (3) SupplierHttpClient.ts — HardenedFetch wrapper with injectable clock/transport, 2200ms default timeout, single jittered retry on 5xx/network error, correlation ID propagation, and full error normalisation; (4) SupplierPort.ts — interface with SupplierFlowShape discriminant (INSTANT/RESERVE_THEN_CONFIRM/ARI_PUSH), SearchCriteria discriminated union that intersects @travel/contracts Zod-inferred request types with a kind tag (no field duplication), and optional reserve()/confirm() for multi-step flows. A reusable contract-compliance Jest harness and four JSON fixtures complete the package.

## WO-051: User Story: WO-051 - Build @travel/queue port with RabbitMQ and SQS adapters
- **Status:** completed
- **Commit:** `15e16c2`
- **Files:** 25 (+2211/-0)
- **Duration:** 913ss
- **Approach:** Built the @travel/queue shared package in four layers: (1) schema — QueueMessageEnvelopeSchema added to @travel/contracts events namespace as the authoritative wire format, with EventType enum covering all four domain event types; (2) port — QueuePort interface with publish/subscribe/isHealthy/close, AckHandle for explicit ack/nack, and QueueValidationError for pre-I/O envelope rejection; (3) adapters — RabbitMqAdapter (durable topic exchange, publisher confirms, prefetch 10, manual ack, 5-attempt DLQ routing, injected connection) and SqsAdapter (AWS SDK v3, MessageGroupId=userId for FIFO ordering per traveller, MessageDeduplicationId=eventId, WaitTimeSeconds=20 long polling, ChangeMessageVisibilityCommand for exponential backoff, silent empty-receive handling); (4) infrastructure — Terraform SQS module with FIFO+DLQ, redrive policy (maxReceiveCount=5), KMS encryption using existing sqs CMK, least-privilege IAM policies split by role (publisher/consumer/DLQ), and CloudWatch alarms at thresholds specified in AC7.

## WO-064: User Story: WO-064 - Scaffold Next.js traveler app shell and design system
- **Status:** completed
- **Commit:** `378f1ce`
- **Files:** 44 (+2736/-6)
- **Duration:** 1208ss
- **Approach:** Extended the existing frontend/ Next.js workspace package (name: 'web') with the full App Router shell in four layers: (1) config — updated tsconfig.json with @/* path aliases, vitest.config.ts for jsdom+RTL+MSW, package.json with Tailwind/Radix/CVA/MSW/RTL/Husky deps, next.config.js to run env validation at startup, plus postcss.config.js, .eslintrc.json, .prettierrc.json, and a Husky pre-commit hook; (2) design tokens — tailwind.config.ts extends Tailwind theme with CSS-custom-property-backed color scales (brand/neutral/success/warning/error/info), typography, radii, shadows, and breakpoints; globals.css declares the custom properties and Tailwind layer directives; (3) primitives — 11 UI components in components/ui/ (Button, Input, Select, Checkbox, Modal, Drawer, Toast+useToast, Card, Badge, Skeleton, EmptyState) using class-variance-authority variant maps and React.forwardRef; Modal and Drawer built on @radix-ui/react-dialog for native focus trap, scroll lock, and ARIA; (4) layout shell — SiteHeader (logo, primary nav, locale/currency switcher placeholder, sign-in link, hamburger), SiteFooter (legal links), MobileNavDrawer (closes on Escape via Radix, closes on route change via usePathname effect, closes on resize above 768px), Providers client wrapper for ToastProvider, and RootErrorBoundary class component; all wired in app/layout.tsx as a Server Component root; (5) API layer — lib/api/errors.ts defines ApiError class with status/code/fieldErrors/retryable, lib/api/client.ts wraps fetch with replaceable token provider, query-string serialization, non-JSON body safety, and on401 hook; lib/env.ts validates NEXT_PUBLIC_API_BASE_URL with Zod and throws on missing variables.

## WO-072: User Story: WO-072 - Expand Schema for Idempotency, Append-Only Audit, and Provenance
- **Status:** completed
- **Commit:** `656b47b`
- **Files:** 25 (+1801/-0)
- **Duration:** 1064ss
- **Approach:** Delivered the expand half of the expand/contract migration pattern. Created the canonical Prisma schema (prisma/schema.prisma) with ProcessedEvent (unique constraint on provider+event_id), extended BookingAuditLog (actor/resource columns + BIGINT identity sequence), and extended Booking (provenance VARCHAR(64), bookable BOOLEAN DEFAULT false). Produced an idempotent raw-SQL migration adding the append-only trigger (enforce_audit_log_append_only), REVOKE UPDATE/DELETE on booking_audit_log, and per-role GRANTs. Added BookingProvenanceSchema + isSupplierProvenance() and the audit payload sanitiser (sanitiseAuditPayload/toSanitisedPayload) to @travel/contracts. Added OFFER_NOT_BOOKABLE (422) error code and offerNotBookable/egressDenied domain error factories. Created injectable AuditWriter.ts and ProvenanceGuard.ts in booking-service and ProcessedEventRepository.ts in payment-service. All changes are purely additive.

## WO-076: User Story: WO-076 - Provision RDS Proxy and Enforce Per-Service Connection Ceilings
- **Status:** completed
- **Commit:** `f4b5db7`
- **Files:** 24 (+2281/-5)
- **Duration:** 912ss
- **Approach:** Provisioned RDS Proxy in a new Terraform module (infra/terraform/modules/rds-proxy/) with IAM auth required, TLS required, and Secrets Manager-backed credentials. Created per-service rds-db:connect IAM policies (one per service, scoped to each service's dedicated PostgreSQL role) plus separate policies for the migration task and purge worker. Wired the proxy into both staging and production environment root modules. Extended the ecs-service module to accept and attach rds-db:connect policy ARNs to task roles. Centralised all connection-string construction in a new @travel/config package (buildDatabaseUrl, assertConnectionLimit) that enforces connection_limit=5 at startup and is the only place connection strings are built — eliminating per-service drift. Added a Postgres 16 service to docker-compose.yml with the same connection_limit=5 so local dev pool behaviour matches production. Built a k6 load-test harness (search and checkout scenarios, committed fixture payloads, configurable rate/duration) with db_pool_errors custom counter and p95 thresholds matching the R9 exit criterion. Added four CloudWatch alarms in monitoring.tf. Documented the connection budget arithmetic (7 DB services × max tasks × 5 = 260 planned, 360 available at 20% MaxConnectionsPercent, 100-connection headroom) and authored the connection-governance runbook.

## WO-079: User Story: WO-079 - Terraform network foundation and remote state backend
- **Status:** completed
- **Commit:** `50eadd9`
- **Files:** 24 (+1445/-44)
- **Duration:** 981ss
- **Approach:** Completed the infra/terraform/modules/network module (vpc.tf, subnets.tf, nat.tf, security_groups.tf, outputs.tf, versions.tf) and expanded variables.tf with the full tag contract and subnet variables. Updated the pre-existing route-tables.tf to remove the duplicate terraform block, route both private-app and private-data tiers through NAT gateways via AZ-local modulo routing, and reference the new internal resources instead of input variables. Created a bootstrap stack (local state, prevent_destroy) for S3 + DynamoDB state backend provisioning. Provisioned three environment root stacks — dev (new, single NAT), staging (network.tf additive), production (network.tf additive) — each instantiating the identical module block with only tfvars values differing. Updated staging and production backend.tf files to pin required_version = "~> 1.15" and aws ~> 5.0 and expand default_tags to the five-key tag contract. Committed non-overlapping CIDR tfvars for all three environments. Added a CI version-check shell script and a Terratest suite asserting subnet counts, NAT gateway count, SG ingress rules, flow logs, and DataClassification=Synthetic for non-production data subnets.

## WO-090: User Story: WO-090 - Containerized Local Infrastructure Stack via Docker Compose
- **Status:** completed
- **Commit:** `2e115aa`
- **Files:** 8 (+860/-37)
- **Duration:** 455ss
- **Approach:** Rewrote docker-compose.yml removing the deprecated top-level version key, added Redis 7.4 as the missing third dependency, pinned all images to exact minor tags, added a named bridge network (travel-local) for stable DNS, per-service healthchecks with start_period tuned for cold starts, deploy resource limits keeping the stack under 2 GB reserved, and an opt-in LocalStack profile for AWS emulation. Created infra/local/ with PostgreSQL init SQL (idempotent role + extension setup), RabbitMQ rabbitmq.conf loading definitions.json (pre-declares travel.events topology matching RabbitMqAdapter defaults), and LocalStack init scripts for SQS FIFO queues and Secrets Manager placeholders. Wrote scripts/wait-for-stack.sh with Docker health polling then protocol-level probes (SQL SELECT 1, Redis PING, rabbitmq-diagnostics check_running), bounded timeout, and per-container diagnostics on failure. Documented everything in docs/local-development.md including the port map, connection strings, max_connections budget arithmetic, dependency-failure drill commands, and CI integration snippet.

## WO-091: User Story: WO-091 - pnpm Workspaces and Turborepo Task Graph with Remote Caching
- **Status:** completed
- **Commit:** `e8f9ceb`
- **Files:** 34 (+1523/-103)
- **Duration:** 833ss
- **Approach:** Established pnpm 9.x workspaces as the single package topology with a 36-entry version catalog pinning every shared runtime and build-tool dependency. Layered Turborepo 2.x task definitions on the resulting dependency graph with correct dependsOn edges, narrowed inputs, declared outputs, and cacheability flags. Enforced the catalog with a TypeScript workspace-lint CLI (catalog-drift, workspace-protocol, engines-consistency checks) with fixture-backed unit tests. Pinned toolchain versions via packageManager, engines fields, and an install-time preinstall guard. Configured remote cache with signature-key injection via environment variables with graceful degradation when unavailable.

## WO-003: User Story: WO-003 - Enforce Zod request validation at all service boundaries
- **Status:** completed
- **Commit:** `9d2f382`
- **Files:** 43 (+2334/-12)
- **Duration:** 1053ss
- **Approach:** Built a single reusable schema-driven middleware factory (validateRequest) in shared/middleware/ that accepts optional body/query/params/headers Zod schemas, parses each location in one pass (single safeParse per location — coerced output cached, not re-parsed), attaches typed results to req.validated on success, and short-circuits with the WO-002 serialiseError 400 envelope on failure without calling next(). Applied it to all six active service boundaries: search-service (flights/hotels/cars), auth-service, user-service, booking-service, payment-service (intent + raw-body webhook), and ai-service (Claude tool registry with input_schema generated from @travel/contracts Zod schemas via an inline JSON Schema converter). Tests cover AC3/4/5 with spy assertions proving no adapter calls on invalid input, AC7 with raw Buffer preservation, AC8 with contracts schema correspondence, AC9 with an 8ms benchmark, AC10 with an import-boundary filesystem scan, and AC11 with 18 middleware unit tests.

## WO-007: User Story: WO-007 - Propagate correlation IDs and expose trace ID as error reference
- **Status:** completed
- **Commit:** `0aa33fb`
- **Files:** 16 (+1367/-69)
- **Duration:** 1098ss
- **Approach:** Implemented correlation ID propagation in three layers: (1) packages/observability/src/correlation.ts — a self-contained module with a Crockford Base32 ULID generator (crypto.randomBytes, 26-char), isValidCorrelationId (ULID/UUID v4 allow-list, CRLF injection guard, max 64 chars), AsyncLocalStorage store, getCorrelationId/getTraceId (opportunistic OTel span context via @opentelemetry/api with fallback to correlation ID so reference is never null), injectHeaders/injectMessageAttributes helpers, and createCorrelationIdMiddleware factory (validates inbound x-correlation-id, never echoes malformed values, echoes resolved ID on response, attaches req.correlationId + req.log child logger, writes to OTel baggage via propagation API, runs chain inside ALS context). (2) shared/errors/AppError.ts — class-based discriminated error hierarchy (10 subclasses) compatible with existing serialiseError isDomainError dispatch. (3) Queue hop: TraceContext interface added to QueuePort, publish() extended with optional TraceContext, both adapters extract traceparent from message attributes/headers and pass TraceContext as third arg to MessageHandler; notification-service consumer re-establishes correlation context before handling. (4) shared/middleware/errorHandler.ts rewritten: resolves reference from req.correlationId > x-correlation-id > x-trace-id headers; logs warn for 4xx, error for 5xx.

## WO-008: User Story: WO-008 - Bootstrap OpenTelemetry tracing with X-Ray propagation
- **Status:** completed
- **Commit:** `76a6b20`
- **Files:** 33 (+1302/-10)
- **Duration:** 1169ss
- **Approach:** Replaced the non-functional shared/instrumentation.ts sketch (which didn't exist) with a correct OpenTelemetry bootstrap inside @travel/observability. Core deliverables: (1) packages/observability/src/tracing.ts exports initTracing(options) and shutdownTracing(sdk) — builds a NodeSDK with AWSXRayIdGenerator, CompositePropagator(X-Ray + W3C traceparent + W3C baggage), OTLP/HTTP exporter to localhost:4318 (overridable via OTEL_EXPORTER_OTLP_ENDPOINT), AlwaysRecordSampler wrapping ParentBased(TraceIdRatioBased(0.1)) so all spans are created in memory (RECORD_ONLY for 90%, RECORD_AND_SAMPLED for 10%), and a custom ErrorAndSlowSpanProcessor that promotes RECORD_ONLY spans to export if they have SpanStatusCode.ERROR or duration > latencyBudgetMs. (2) packages/observability/src/spanAttributes.ts exports three domain helpers: recordSupplierCall, recordBookingTransition, recordAssistantBudget — all safe to call when no span is active, none record PII. (3) Per-service tracing.ts bootstrap files for all 9 services; each calls initTracing with the service name matching the CloudWatch log group. (4) Service index.ts files updated to import tracing.ts first so auto-instrumentation patches Express/Prisma/Redis/amqplib before they load. (5) ADR 0002 records the sampling policy rationale and ~$240/month telemetry cost estimate. (6) Unit tests using InMemorySpanExporter cover sampler decisions, span processor logic, propagator inject/extract, resource attributes, and all three domain span helpers.

## WO-009: User Story: WO-009 - Implement dependency-aware deep health and readiness endpoints
- **Status:** completed
- **Commit:** `e4b8c9b`
- **Files:** 16 (+1420/-16)
- **Duration:** 1102ss
- **Approach:** Built a generic probe-based health builder in @travel/observability with per-probe timeouts (Promise.race vs setTimeout), in-memory TTL cache keyed by probe name, and Promise.allSettled aggregation. Handlers are typed against node:http IncomingMessage/ServerResponse so they work in both Express (where Response extends ServerResponse) and raw http.createServer(). Required probe failures → 503 unhealthy; non-required failures → 200 degraded to prevent ECS deployment circuit-breaker rollbacks on cache/queue outages. Added a startup secrets validator with a placeholder deny-list that exits process.exit(1) before the HTTP listener binds, logging only the variable name (never the value). Wired /health/live and /health/ready into all 9 services with an optional HealthHandlers parameter to preserve backward compatibility with existing tests.

## WO-012: User Story: WO-012 - Fail-Fast Startup Secret Validator Across All Services
- **Status:** completed
- **Commit:** `80b7639`
- **Files:** 23 (+1001/-0)
- **Duration:** 983ss
- **Approach:** Built a pure, dependency-injectable SecretValidator module in @travel/observability with exact-match placeholder blocklist, per-field minLength rules, allowEmptyInDev exemptions, and relaxed-dev-mode support. The pure validate(manifest, env, opts) function is fully unit-testable with no process control; assertSecretsOrExit wraps it with process.env access, Pino-compatible logging (key names only — never values), and process.exit(1) in strict mode. Declarative per-service manifests enumerate each service's required secrets with typed SecretDescriptor. Added createValidatorProbe to the health builder using dynamic import to avoid circular graph. Extended Pino redaction paths to cover 20+ secret/token/key/password field patterns. Wired assertSecretsOrExit into all 9 service index.ts files after the tracing bootstrap, gated with NODE_ENV !== 'test' so unit test suites importing createApp continue to work.

## WO-017: User Story: WO-017 - Gateway Edge Hardening: Headers, CORS, Cookies, CSRF
- **Status:** completed
- **Commit:** `7732e5a`
- **Files:** 19 (+1705/-0)
- **Duration:** 914ss
- **Approach:** Created the api-gateway service from scratch with a layered edge security middleware stack mounted in deliberate order. Added TOKEN_REVOKED and CSRF_FAILED to the shared error contracts (with exhaustive ERROR_STATUS_MAP entries). The middleware chain strips inbound x-internal-actor headers first, then sets security headers, enforces CORS via a per-environment typed allow-list, routes /webhooks/stripe through a raw body handler before the global JSON parser, and authenticates requests using RS256 JWT verification through the KeyProvider verification key set with Redis jti denylist and conservative deny-on-unavailability policy. CSRF uses the double-submit cookie pattern with Origin and Sec-Fetch-Site defense-in-depth; the webhook route is exempt. JWT verification uses Node.js built-in crypto (RSA-SHA256) with no new external dependency. Comprehensive unit tests run fully offline using ephemeral RSA key pairs generated at test runtime.

## WO-024: User Story: WO-024 - Implement logout, session revocation, and password reset
- **Status:** completed
- **Commit:** `0f010bf`
- **Files:** 25 (+1947/-21)
- **Duration:** 1406ss
- **Approach:** Built the full logout/session-revocation/password-reset stack on top of the existing auth-service skeleton. Since related WOs (018-023) were not yet in the branch, built the full infrastructure from scratch: (1) duck-typed SessionRepository and OneTimeTokenRepository interfaces with concrete implementations injected at runtime; (2) SessionService centralising all revocation paths with an injectable SessionCacheInvalidator hook; (3) PasswordResetService with fire-and-forget forgot-password (enumeration-safe) and a fully transactional reset-password (token validation, scrypt-based reuse check, lockout clear, sibling-token invalidation, session revoke, post-commit mailer); (4) requireAuth middleware reading x-internal-actor forwarded by the api-gateway; (5) in-memory sliding-window rate limiter injectable for production Redis replacement; (6) Node.js crypto.scrypt for password hashing (no external library — not in catalog); (7) Prisma schema extended additively with Session metadata columns, User lockout fields, and a new OneTimeToken model with composite index.

## WO-026: User Story: WO-026 - Build Amadeus flight supplier adapter
- **Status:** completed
- **Commit:** `e31f375`
- **Files:** 17 (+2118/-0)
- **Duration:** 1423ss
- **Approach:** Created a new services/flight-service/ module containing three injectable units: (1) AmadeusTokenProvider — OAuth2 client-credentials token manager with Redis caching (key: supplier:amadeus:token, TTL = expires_in − 60 s), forced-refresh on 401, and alertable warn logs for credential failures; (2) amadeusOfferMapper — pure mapper translating Amadeus v2 flight-offer payloads to port-neutral Offer objects with deterministic sha256 IDs, ISO-duration parsing, cabin-class normalisation, expiresAt from lastTicketingDate/lastTicketingDateTime with a configurable default-validity fallback, provenance: AMADEUS, bookable: true; (3) AmadeusFlightAdapter — implements SupplierPort with flowShape INSTANT, wires tokenProvider + SupplierHttpClient + mapper behind searchOffers(), wraps each call in an OTel span (supplier.amadeus.search) with attributes offersReturned/httpStatus/retried/durationMs, and handles 401 mid-flight by refreshing the token and retrying the search once. No process.env reads inside adapters; all credentials flow via injected AmadeusTokenConfig. A composition root (createAmadeusFlightAdapter) wires hardenedFetch, EgressAllowList, SupplierHttpClient, and AmadeusTokenProvider with production defaults. Amadeus hostnames (api.amadeus.com, test.api.amadeus.com) are documented in AMADEUS_ALLOWED_HOSTS and already present in @travel/suppliers ALLOWED_DESTINATIONS. vitest.config.ts uses resolve.alias to point workspace packages at their TypeScript sources, avoiding dist-build requirements for tests.

## WO-027: User Story: WO-027 - Build RapidAPI hotel supplier adapter
- **Status:** completed
- **Commit:** `c0697c7`
- **Files:** 16 (+1706/-0)
- **Duration:** 937ss
- **Approach:** Created a new services/hotel-service/ module containing two injectable units: (1) rapidApiHotelMapper — pure mapper translating RapidAPI hotel provider responses to port-neutral Offer objects with star-rating coercion (numeric, string, half-star, null → clamped number or null), optional review signal passthrough (review_score in details, review_nr in reviews — never synthesised when absent), deterministic sha256 offer IDs (RAPIDAPI + hotelId + ratePlanCode + searchFingerprint), nightly-price derivation from total price and stay nights (Math.round(total/nights*100)/100, documented in code, minimum 1 night), expiresAt = now + defaultValidityMinutes, provenance RAPIDAPI, bookable true, freshness FRESH; (2) RapidApiHotelAdapter — implements SupplierPort with flowShape INSTANT, sends x-rapidapi-key and x-rapidapi-host headers (injected from config, never logged or in spans), wraps calls in OTel span supplier.rapidapi-hotel.search with attributes offersReturned/httpStatus/retried/durationMs, remaps HTTP 429 from SupplierRejectedRequestError to SupplierUnavailableError with distinct log code SUPPLIER_QUOTA_EXHAUSTED + alertable flag, propagates all other errors from SupplierHttpClient as-is. A composition root (createRapidApiHotelAdapter) wires hardenedFetch, EgressAllowList, SupplierHttpClient (2200ms timeout), and RapidApiHotelAdapter. RapidAPI wildcard hostname *.p.rapidapi.com already in @travel/suppliers ALLOWED_DESTINATIONS. vitest.config.ts uses resolve.alias for workspace packages.

## WO-028: User Story: WO-028 - Build RapidAPI car rental supplier adapter
- **Status:** completed
- **Commit:** `cdb4e8e`
- **Files:** 18 (+1946/-0)
- **Duration:** 809ss
- **Approach:** New services/car-service/ module following the same pattern as WO-027 hotel-service. Provider confirmed as RapidAPI (not Priceline) based on existing RAPIDAPI_KEY in Secrets Manager and pre-approved *.p.rapidapi.com egress. Core components: vehicleClassMap.ts (data-driven token table with UNKNOWN fallback), rapidApiCarMapper.ts (pure function, total price derived from dailyRate*rentalDays when total absent), RapidApiCarAdapter.ts (SupplierPort, INSTANT flow, OTel span supplier.rapidapi-car.search with unmappedClassCount attribute, warn log per unmapped description, no 429 remapping unlike hotel adapter), composition.ts (factory wiring SupplierHttpClient + EgressAllowList). 91 tests across 4 suites: vehicleClassMap unit (32), mapper unit (20), adapter unit (18), contract-compliance (21).

## WO-030: User Story: WO-030 - Gate illustrative results behind audited feature flag
- **Status:** completed
- **Commit:** `756c325`
- **Files:** 15 (+1537/-0)
- **Duration:** 713ss
- **Approach:** New packages/illustrative/ package implementing a feature-flagged illustrative offer generator with append-only audit trail. Core components: (1) gating.ts — pure evaluateGating() function over the matrix of hardDisabledEnvironments × flagEnabled × supplierWasUnavailable, returning an explicit allow/suppress decision with reason; (2) IllustrativeOfferGenerator implements SupplierPort (INSTANT), with layered defence-in-depth: hard-disable check → flag check (flag errors treated as disabled) → deterministic offer generation → audit write (fail-closed: if write fails, returns [] and emits alertable error log) → OTel counter emission; (3) DegradedResultBlockSchema added to @travel/contracts for the response-level illustrative indicator (AC6). Deterministic generation uses SHA-256 fingerprint of normalised criteria as a seed; same query always produces the same 3 offers per category (flight/hotel/car templates with seed-derived price offset). FakeFeatureFlagProvider and FakeAuditWriter committed for test isolation.

## WO-032: User Story: WO-032 - Implement offer normaliser and preference-aware ranker
- **Status:** completed
- **Commit:** `1d33461`
- **Files:** 18 (+1756/-0)
- **Duration:** 1217ss
- **Approach:** New packages/offer-normaliser package — pure business logic, no infrastructure imports. Two classes: (1) OfferNormaliser accepts ReadonlyArray<unknown> (raw fan-out output), validates each offer with OfferSchema.safeParse() from @travel/contracts, drops failures with a structured warn log, excludes offers with expiresAt <= clock.now() and emits OTel counter 'offers_excluded_expired_total' with {category, supplier} dimensions; (2) OfferRanker applies a hard bookable-before-non-bookable partition then scores each group with normalised price component (priceWeight*(maxPrice-price)/range) plus preference bonuses (cabinClass, hotelStars>=min, carClass, homeAirport), sorts score DESC with documented tie-break price ASC→supplier ASC→id ASC, and optionally attaches rankingExplanation per offer when config.exposeRankingExplanation=true. DEFAULT_RANKING_WEIGHTS exported as reference defaults; validateWeights() throws on startup if any weight is non-finite or negative.

## WO-052: User Story: WO-052 - Rebuild notification consumer with exactly-once SES delivery
- **Status:** completed
- **Commit:** `be1fbe4`
- **Files:** 28 (+2860/-91)
- **Duration:** 1326ss
- **Approach:** Rebuilt notification-service as a layered hexagonal architecture: (1) domain/backoff.ts defines typed error classes and capped-jitter backoff; (2) repositories/NotificationProcessedEventRepository catches Prisma P2002 unique violations as 'duplicate' signals; (3) repositories/SuppressionRepository stores SHA-256 email hashes — never raw addresses; (4) domain/IdempotencyGuard composes Redis SET NX (72h TTL, hot path) + DB insert (durable authority) so a Redis outage degrades to DB-only without dropping or duplicating messages; (5) adapters/SesEmailAdapter wraps SESv2Client translating AWS error names to typed error classes; (6) domain/NotificationDispatcher orchestrates the full pipeline (validate payload → check suppression → commit DB row → SES send) with the DB insert committed BEFORE the SES dispatch to guarantee crash safety; (7) routes/health.ts builds the deep readiness probe with postgres+redis+queue+secrets all required=true; (8) consumer.ts wires QueuePort.subscribe with nack(false) for non-retryable/exhausted errors → DLQ and nack(true) for remaining transients. Terraform: independent ECS service module call, step-scaling autoscaling target keyed to ApproximateNumberOfMessagesVisible>100, least-privilege IAM (ses:SendEmail + SQS consumer actions only), CloudWatch dashboard. SES config set, SNS bounce/complaint topic, email templates, and suppression-feed SQS queue in ses.tf.

## WO-065: User Story: WO-065 - Build traveler authentication and session management flows
- **Status:** completed
- **Commit:** `81d04c8`
- **Files:** 29 (+2722/-23)
- **Duration:** 968ss
- **Approach:** Implemented a complete auth and session management layer for the Next.js 14 App Router frontend. Token material is confined exclusively to an HMAC-SHA256 signed httpOnly cookie — it never reaches localStorage, sessionStorage, or any non-httpOnly cookie. Server actions own all token exchange and cookie writes. A client-side SessionProvider is hydrated from a stripped ClientSession (user profile + expiry, no tokens) rendered server-side. The API client's 401 path was extended to support an async retry-capable On401Handler, wiring in a single-flight refresh helper that deduplicates concurrent 401 responses via a shared Promise. Middleware reads the signed cookie directly (no DB call) and redirects unauthenticated visitors to /sign-in with a sanitized returnTo parameter.

## WO-071: User Story: WO-071 - Harden web performance, accessibility, SEO, and observability
- **Status:** completed
- **Commit:** `b5c0a7b`
- **Files:** 28 (+2822/-9)
- **Duration:** 957ss
- **Approach:** Implemented the full WO-071 hardening pass across six domains: (1) Performance CI — lighthouserc.json with per-route budgets (LCP≤2500ms, CLS≤0.1, TBT≤300ms, bytes≤512KB) and @next/bundle-analyzer behind ANALYZE flag; (2) Image optimization and security headers in next.config.js plus Cache-Control: no-store for /checkout and /account, supplemented by force-dynamic exports in those pages; (3) SEO — JSON-LD serializers for Organization/BreadcrumbList/LodgingBusiness/Product/FAQPage (aggregateRating omitted for zero reviews), noindex metadata for checkout/account/auth, dynamic sitemap with locale alternates, robots.ts; (4) Observability — Core Web Vitals reporter via sendBeacon, error reporter with PII scrubbing and release tagging; (5) Analytics — typed event schema with PII scrubber removing TOKEN_KEYS/PAYMENT_KEYS and masking emails/card numbers, batching client with pagehide flush; (6) Testing — axe-core helper, Playwright e2e specs for auth/booking/cancellation, and unit tests for all new modules.
