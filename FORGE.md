# Forge Implementation Log

| Field | Value |
|-------|-------|
| Project | 40ac2648-20da-4cb9-843b-a9ea0b666bfa |
| Branch | forge/voya-9ddc7ab4-run25-106wo |
| Started | 2026-07-31T05:02:15Z |

---

## WO-002: User Story: WO-002 - Standard error envelope and HTTP status mapping contract
- **Status:** completed
- **Commit:** `7d18785`
- **Files:** 24 (+1574/-0)
- **Duration:** 930ss
- **Approach:** Added the platform-wide error contract to @travel/contracts as a self-contained errors/ module. envelope.ts defines the Zod strict schema producing exactly {error:{code,message,field?},reference}. codes.ts declares ErrorCode as a const-union and ERROR_STATUS_MAP as Record<ErrorCode,number> (compile-time exhaustive). domain-errors.ts exports 11 typed factory helpers. serialise.ts implements three dispatch paths (ZodError→VALIDATION_FAILED, DomainError→code lookup, unknown→fixed INTERNAL_ERROR) with a RESTRICTED_FIELDS redaction list mirroring Pino paths, and a sortable fallback correlation ID so the package stays free of OpenTelemetry. shared/middleware/errorHandler.ts is a thin Express adapter calling serialiseError and writing status+JSON. Unit tests cover envelope shape, exhaustive status mapping, serialiser paths, per-field redaction, and leakage prevention. An integration test in booking-service wires CreateBookingRequestSchema failures through serialiseError end-to-end.

## WO-004: User Story: WO-004 - Migrate Next.js frontend onto shared contract types
- **Status:** completed
- **Commit:** `aa67802`
- **Files:** 23 (+1355/-4)
- **Duration:** 811ss
- **Approach:** Built the frontend contracts migration in five layers: (1) configuration — tsconfig.json (strict, react-jsx, bundler resolution) and next.config.js (transpilePackages for workspace ESM); (2) types/index.ts — presentational-only types (BookabilityState, FormErrorState, ApiResult discriminated union) with zero platform payload shapes; (3) pure TypeScript library modules — api-client.ts (typed fetch wrapper returning ApiResult<T>, parses 2xx against contracts schemas and non-2xx against ErrorEnvelopeSchema, synthesises valid envelope for non-JSON gateway responses), error-mapper.ts (mapEnvelopeToFormErrors with top-level segment matching and form-level banner fallback), offer-guard.ts (getBookabilityState / canInitiateCheckout gating ILLUSTRATIVE and expired offers structurally); (4) App Router pages and OfferCard component — all importing types exclusively from @travel/contracts; (5) unit tests for all three library modules and fixture JSON files for search responses, offer lists, and error envelopes.

## WO-005: User Story: WO-005 - Contract compatibility test harness and CI gate
- **Status:** completed
- **Commit:** `bce0c0b`
- **Files:** 70 (+2874/-5)
- **Duration:** 2000ss
- **Approach:** Built the contract compatibility harness in five layers: (1) schema registry — src/registry.ts maps 23 stable dot-notation IDs to every public Zod schema, with a completeness test asserting no gaps or stale baselines; (2) Zod-to-JSON-Schema converter — scripts/zod-to-json-schema.ts implements a recursive Zod _def inspector producing deterministic, sorted JSON Schema fingerprints covering all Zod types used in the codebase (ZodEffects/Pipeline unwrapping, ZodObject strict-mode, ZodEnum sorted values, ZodArray minItems, ZodRecord, ZodUnion oneOf); (3) committed baselines — 23 JSON Schema baseline files in contract-baselines/ generated by tracing each schema through the converter, plus a phase0-metrics.json recording the pre-harness state; (4) test infrastructure — comparator test regenerates every baseline in memory and fails with the schema ID and regeneration instructions on any divergence; classifier.ts categorises diffs as additive/breaking/no-op with a version gate that rejects breaking changes without a major-version bump; test-helpers.ts isolates all vitest assertions so a runner switch only updates one file; (5) consumer fixture tests — contracts.consumer.test.ts added for all 9 services and the frontend, each parsing its specific fixture files against the schemas it depends on, so a narrowing change fails at the consumer rather than only in the contracts package.

## WO-006: User Story: WO-006 - Build @travel/observability Pino logger with PII redaction
- **Status:** completed
- **Commit:** `2376a8a`
- **Files:** 17 (+1200/-5)
- **Duration:** 1119ss
- **Approach:** Created the @travel/observability workspace package with a Pino JSON logger factory and exhaustive PII redaction, then enabled the no-console ESLint rule across all package/service source directories. The package has three source modules: context.ts (typed LogContext interface with no index signature), logger.ts (createLogger + createChildLogger with LOG_LEVEL validation, ISO-8601 timestamps, base bindings, redact config at construction, and a custom error serializer that scrubs email and digit patterns from stack strings), and index.ts (barrel). Tests use Jest 29 + ts-jest in CommonJS mode to avoid ESM/Jest experimental flags while the package itself ships as ESM. An ADR records the two ratified parameters: 30-day CloudWatch application log retention (explicitly distinct from the 1-year immutable audit record store) and Jest 29 as the epic test runner. The root eslint.config.js and per-package .eslintrc.cjs files enforce no-console across all source directories. shared/middleware/errorHandler.ts was updated to accept an optional ErrorLogger injection parameter to enable structured error logging (wired fully in WO-007).

## WO-013: User Story: WO-013 - Secrets Manager Injection, KMS Rotation, and Leak Gating
- **Status:** completed
- **Commit:** `58bcd6e`
- **Files:** 27 (+2113/-6)
- **Duration:** 817ss
- **Approach:** Implemented the full secrets-injection and rotation infrastructure in three layers: (1) Terraform modules — kms (one CMK per store with explicit key policies), secrets (11 logical credentials with lifecycle ignore_changes and 90-day rotation), ecs-service (task definition with secrets block + least-privilege execution role IAM); (2) staging/production environment roots wiring the modules together with S3+DynamoDB Terraform state backends and data-source-driven ECS cluster/subnet lookups; (3) @travel/auth TypeScript package exposing a dependency-injected KeyProvider with two-phase JWT rotation (24-hour overlap window), in-memory cache with configurable TTL, and a SecretsManagerKeyStore production implementation. Added scan:gitleaks as a blocking parallel pipeline step alongside semgrep/snyk/sonarqube/grype, plus Terraform plan stages. Security docs and a .env build-context assertion script round out the deliverables.
