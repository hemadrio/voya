{
  "name": "reporting-service",
  "version": "0.1.0",
  "private": true,
  "description": "Reporting service \u2014 generates booking analytics and audit reports.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@travel/contracts": "workspace:*",
    "@travel/observability": "workspace:*"
  },
  "devDependencies": {
    "@travel/test-config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=20.0.0 <21.0.0"
  }
}
