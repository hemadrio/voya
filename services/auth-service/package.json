{
  "name": "auth-service",
  "version": "0.1.0",
  "private": true,
  "description": "Auth service \u2014 handles registration, login, JWT refresh, and OAuth.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@travel/contracts": "workspace:*",
    "@travel/observability": "workspace:*",
    "@travel/ratelimit": "workspace:*",
    "express": "catalog:",
    "@prisma/client": "catalog:"
  },
  "devDependencies": {
    "@travel/test-config": "workspace:*",
    "@types/express": "catalog:",
    "@types/supertest": "catalog:",
    "supertest": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=20.0.0 <21.0.0"
  }
}
