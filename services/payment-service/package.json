{
  "name": "payment-service",
  "version": "0.1.0",
  "private": true,
  "description": "Payment service \u2014 creates Stripe PaymentIntents and processes webhooks.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@travel/contracts": "workspace:*",
    "@travel/observability": "workspace:*",
    "@travel/queue": "workspace:*",
    "express": "catalog:"
  },
  "devDependencies": {
    "@travel/test-config": "workspace:*",
    "@types/express": "catalog:",
    "@types/supertest": "catalog:",
    "supertest": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=20.0.0 <21.0.0"
  }
}
