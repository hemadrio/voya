{
  "name": "api-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "API gateway \u2014 edge hardening: security headers, CORS allow-list, RS256 JWT verification, jti denylist, CSRF, and raw-body webhook passthrough.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@travel/auth": "workspace:*",
    "@travel/contracts": "workspace:*",
    "@travel/observability": "workspace:*",
    "@travel/ratelimit": "workspace:*",
    "express": "catalog:",
    "ioredis": "catalog:"
  },
  "devDependencies": {
    "@travel/test-config": "workspace:*",
    "@types/express": "catalog:",
    "@types/node": "catalog:",
    "@types/supertest": "catalog:",
    "supertest": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=20.0.0 <21.0.0"
  }
}
