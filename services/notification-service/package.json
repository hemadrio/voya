{
  "name": "notification-service",
  "version": "0.1.0",
  "private": true,
  "description": "Notification service — consumes SQS events and dispatches emails via SES.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-sesv2": "catalog:",
    "@prisma/client": "catalog:",
    "@travel/contracts": "workspace:*",
    "@travel/observability": "workspace:*",
    "@travel/queue": "workspace:*",
    "ioredis": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=20.0.0 <21.0.0"
  }
}
